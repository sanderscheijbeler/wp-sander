{% extends "base.twig" %}

{% block content %}
    <div class="container">
        <div class="row">
            {% for post in posts %}
                <div class="col-12">
                    {% include ["tease-"~post.post_type~".twig", "tease.twig"] %}
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
