{% extends "base/base.twig" %}

{% block content %}
    <div class="container">
        <div class="row">
            {% for post in posts %}
                <div class="col-24">
                    {% include ["card/tease-"~post.post_type~".twig", "card/tease.twig"] %}
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
